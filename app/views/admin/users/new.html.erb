h2
|  Новый пользователь
|  <%= form_for @user, url: admin_users_path do |f| %> <% if @user.errors.any? %>
    #error_explanation
    h2
    |  <%= pluralize(@user.errors.count, 'ошибки') %>, запрет на сохранение:
    ul
    |  <% @user.errors.full_messages.each do |msg| %>
      li
      | <%= msg %>
      |  <% end %>
    |  <% end %>
  p
  |  Имя
  br
  |  <%= f.text_field :name %>
  p
  p
  |  Имэйл
  br
  |  <%= f.text_field :email %>
  p
  p
  |  <%= f.submit 'Сохранить'%>
  |  <% end %> <%= link_to 'Назад', admin_users_path %>